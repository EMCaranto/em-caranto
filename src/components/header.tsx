// React JS
import React from 'react'

// Components
import ThemeToggle from '@/components/theme-toggle'

const Header = () => {
  return (
    <div>
      <ThemeToggle />
      Header
    </div>
  )
}

export default Header
